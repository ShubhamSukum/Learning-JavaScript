'use strict';

let Arr=[];

function shubz(cns=40,cc=40,wadl=40,dsbda=40){
    const obj={
        cns,
        cc,
        wadl,
        dsbda
    };

    Arr.push(obj);

    console.log(obj);
}

shubz(58,60,57,58);